name: Scheduled Deploy to Cloudflare Pages

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Cloudflare Pages Build
        run: curl -X POST "${{ secrets.CF_PAGES_DEPLOY_HOOK }}"